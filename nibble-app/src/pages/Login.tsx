import { Link } from "react-router-dom";

const Login = () => {
  return (
    <div>
      <p>Login</p>
      <button>
        <Link to="/">Login</Link>
      </button>
    </div>
  );
};

export default Login;
