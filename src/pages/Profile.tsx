import BottomNavigationBar from "components/BottomNavigationBar";

const Profile = () => {
  return (
    <div>
      <p>Profile</p>
      <BottomNavigationBar />
    </div>
  );
};

export default Profile;
